apiVersion: v1
kind: Secret
metadata:
  name: media-db-secret
  namespace: media
type: Opaque
stringData:
  # 统一的数据库密码，填入你在 pgAdmin 中设置的密码
  postgres-password: "YOUR_SUPER_STRONG_PASSWORD"  # REPLACE_ME